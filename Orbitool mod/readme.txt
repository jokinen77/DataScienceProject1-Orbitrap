Currently the mod in here just provides a function to export all loaded spectra
in two modes: raw (or as is), and denoised (using the parameters in the window).
To make it work just replace the file in this folder with those with the same name
in the Orbitool folder.
